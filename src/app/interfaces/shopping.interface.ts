/**
 * There are the variables that can be used to add the name, price, quantity to the shopping cart or display your information
 */
export interface Shopping{
    amount?: number,
    name?: string,
    price?: number,
    id?:string
}